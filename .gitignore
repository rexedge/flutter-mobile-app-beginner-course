# Dart / Flutter build output — regenerated by `dart run` or `flutter run`.
# Nothing here needs to be in the repo.
.dart_tool/
.packages
build/
*.dart_tool

# Dependency lock files. You'll generate your own the first time you run.
pubspec.lock

# Editor and OS noise
.idea/
.vscode/
*.iml
.DS_Store
Thumbs.db
