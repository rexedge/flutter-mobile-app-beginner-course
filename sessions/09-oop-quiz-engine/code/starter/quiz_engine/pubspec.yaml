name: quiz_engine
description: "Module 1 mini-app — a real, interactive console quiz built with classes, inheritance, and mixins."
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.12.0 <4.0.0'

dev_dependencies:
  lints: ^5.0.0
