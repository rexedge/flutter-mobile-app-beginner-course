# Session 5 — Code

No new project this session. Session 5 keeps editing the same `dart_practice` project from Session 4.

- **`starter/dart_practice/`** — identical to Session 4's finished bio script. If you still have your own, use that instead; this copy is here in case you need a clean one.
- **`final/dart_practice/`** — the same file after this session's changes: a `const appVersion`, two `final` values computed with arithmetic operators, and every `print()` rewritten using string interpolation.

Yours should hold your own details. The shape is what matters, not the values.

## Running either version

```
cd starter/dart_practice     # or final/dart_practice
dart run
```
