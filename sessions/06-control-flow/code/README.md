# Session 6 — Code

Still the same `dart_practice` project, carried forward from Session 5.

- **`starter/dart_practice/`** — identical to Session 5's finished file. If you still have your own, use that instead; this copy is here in case you need a clean one.
- **`final/dart_practice/`** — the same file after this session: an `if`/`else if`/`else` grade calculator, a `while`-loop countdown, and a `for`-loop squares table.

The infinite loop from the break-it section is **not** in `final/` — that file is the clean, correctly-terminating version. Break it yourself, then put the line back.

## Running either version

```
cd starter/dart_practice     # or final/dart_practice
dart run
```

If a program ever won't stop, press Ctrl+C in the terminal.
