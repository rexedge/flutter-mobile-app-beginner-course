name: dart_practice
description: "Dart practice project (Module 1) — plain Dart, no Flutter. Carried forward session to session."
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.12.0 <4.0.0'

dev_dependencies:
  lints: ^5.0.0
